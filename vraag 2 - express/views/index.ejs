<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>MY APP</title>
</head>
<body>
  May Node and Express be with me.

  <ul class="examen">
    <% for(var i=0; i<examen.length; i++) {%>
      <li class="examen">
        <span><%= examen[i].student %></span>
        <span><%= examen[i].examen %></span>
        <span><%= examen[i].reden %></span>
        <span><%= examen[i].datumIndienen %></span>
      </li>
    <% } %>
  </ul>

  <form action="/quotes" method="POST">
    <input type="text" placeholder="student" name="student">
    <input type="text" placeholder="examen" name="examen">
    <input type="text" placeholder="reden" name="reden">
    <button type="submit">Submit</button>
  </form>

</body>
</html>